# VaaniForm Frontend

React + Vite + Tailwind CSS frontend interface for VaaniForm.

## Setup & Running

1. Install npm dependencies:
   ```cmd
   npm install
   ```
2. Start development server:
   ```cmd
   npm run dev
   ```
3. Build for production:
   ```cmd
   npm run build
   ```
